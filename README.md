This is just a bunch of shell scripts and linux config files, X config preferences and stuff like that.
Whenever I use a new linux machine or create a new user, its painful to manually do this every time, so this is just a set of stuff that I can clone and then run it to deploy my preferences.
